{
  "name": "typenihongo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:mobile-game": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test",
    "mobile:start": "npm --prefix mobile start",
    "mobile:typecheck": "npm --prefix mobile run typecheck",
    "mobile:doctor": "npm --prefix mobile run doctor",
    "locale:create": "node scripts/create-locale.mjs",
    "validate:vocabulary": "node scripts/validate-vocabulary.mjs"
  },
  "devDependencies": {
    "vite": "^8.1.1"
  },
  "dependencies": {
    "phaser": "^3.90.0",
    "simple-keyboard": "^3.8.176"
  }
}
