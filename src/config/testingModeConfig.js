export const TESTING_MODE_CONFIG = Object.freeze({
  // Temporary MVP helper. Remove this config and SetupScene usages when testing mode is no longer needed.
  initialEnabled: false,
  labels: Object.freeze({
    enabled: 'TESTING MODE ON',
    disabled: 'TESTING MODE OFF',
  }),
});
